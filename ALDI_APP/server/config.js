const config = {
  database: {
    /* don't expose password or any sensitive info, done only for demo */
    host: "localhost",
    user: "root",
    password: "",
    database: "aldi",
  },
  listPerPage: 10,
};
module.exports = config;